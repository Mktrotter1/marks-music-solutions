server:
  host: "0.0.0.0"
  port: 8080

music:
  directories:
    - "D:/Music"  # Change this to your music library path
  watch_for_changes: true

database:
  path: "data/mms.db"

transcode:
  cache_dir: "data/cache/transcode"
  ffmpeg_path: "ffmpeg"
